import { FormControlErrorMsgPipe } from './form-control-error-msg.pipe';

describe('FormControlErrorMsgPipe', () => {
  it('create an instance', () => {
    const pipe = new FormControlErrorMsgPipe();
    expect(pipe).toBeTruthy();
  });
});
